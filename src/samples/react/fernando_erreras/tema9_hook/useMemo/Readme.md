# React

When a state change al component of to the level of state change too, 
steal the component do not have relation with the state changed

# Memo

This functionality make the component do no reloaded if not involucred wit the state. 
If it's involucred then he will change
